class Product {
  constructor(id, ownerId, title, imageUrl, description, address) {
    this.id = id;
    this.ownerId = ownerId;
    this.imageUrl = imageUrl;
    this.title = title;
    this.description = description;
    this.address = address;

    }
}

export default Product;
